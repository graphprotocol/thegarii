//! arweave client
use crate::result::Result;

/// Arweave client
pub struct Client {
    // fn get_block_by_height(height: u64) -> Result<Block> {
//
// }
// fn get_tx_by_hash(hash: [u8; 32]) -> Result<Transaction> {
// }
// fn get_tx_data_by_hash(hash: [u8; 32]) -> Result<Data> {
//
// }
}
